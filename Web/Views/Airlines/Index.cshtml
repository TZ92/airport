@model IEnumerable<Domain.Entities.Airlinecompany>

@{
    ViewBag.Title = "Index";
}

<h2 style="color:blue"><i class="fa fa-list"></i> List of Airlines</h2>

@using (Html.BeginForm())
{

    <div class="title_right">
        <div class="col-md-5 col-sm-5 col-xs-12 form-group pull-right top_search">
            <div class="input-group">
                <input type="text" name="search" id="search" class="form-control" placeholder="Search By FirstName or LastName">
                <span class="input-group-btn">
                    <button style="margin-left:15px;height:35px" class="btn btn-default" type="submit" value="search"><i class="fa fa-search"></i></button>
                </span>
            </div>
        </div>
    </div>
}


<table id="dynamic-table" class="table table-striped table-bordered table-hover">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Continent)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Contry)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Phone)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SiteWeb)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>

            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Continent)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Contry)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.Phone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SiteWeb)
            </td>
            
            <td>

                <div class="hidden-sm hidden-xs action-buttons">
                    <a class="blue" href="@Url.Action("Details", "Airlines", new { id = item.AirlinecompanyId})">
                        <i class="ace-icon fa fa-search-plus bigger-130"></i>
                    </a>

                    <a class="green" href="@Url.Action("Edit", "Airlines", new { id = item.AirlinecompanyId})">
                        <i class="ace-icon fa fa-pencil bigger-130"></i>
                    </a>

                    <a class="red" href="@Url.Action("Delete", "Airlines", new { id = item.AirlinecompanyId})">
                        <i class="ace-icon fa fa-trash-o bigger-130"></i>
                    </a>
                </div>
               
            </td>
        </tr>
    }

</table>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
